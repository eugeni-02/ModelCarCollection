<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ModelCarCollection" parent="Theme.Material3.Light.NoActionBar">
        <!-- Force light status bar even in dark mode because app UI is hardcoded to light -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">#FFFFFF</item>
    </style>
</resources>